<div class="jumbotron">
  <h2>Tous les évènements :</h2>
</div>

<div class="card-columns">
<% @events.sort_by{|e| e.created_at}.reverse.each do |e| %>
  <%= render partial: 'events/card', locals: {event: e} %>
<% end %>
</div>
